# Go and Windows build artifacts
*.exe
*.exe~
*.log

# Flutter generated files
surge/.dart_tool/
surge/build/
surge/.idea/
surge/.metadata
surge/.flutter-plugins
surge/.flutter-plugins-dependencies

# Local editor and OS files
.DS_Store
Thumbs.db
.vscode/
.kiro/

# Local configs with real keys (never commit)
.env
backend/.env
relayd-ark.yaml
relayd-local.yaml

# Runtime SQLite data (accounts table contains real upstream keys)
data/
